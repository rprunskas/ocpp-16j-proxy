# Changelog

## 0.1.0

- Initial Home Assistant OS app/add-on packaging.
- Added UI configuration for upstream URL, logging, WebSocket timeouts, message
  size, and authorization forwarding.
- Exposed the OCPP listener on TCP port 9000.

